{
  "mcpServers": {
    "figma": {
      "type": "http",
      "url": "https://mcp.figma.com/mcp"
    }
  },
  "_comment": [
    "External Repository Access:",
    "",
    "For local clones of external repos (backend-docs, client-docs, svc-map),",
    "use --add-dir instead of filesystem MCP servers:",
    "",
    "  claude --add-dir /path/to/backend-docs \\",
    "         --add-dir /path/to/client-docs \\",
    "         --add-dir /path/to/svc-map",
    "",
    "--add-dir directories are accessible via Glob, Grep, Read — no MCP needed.",
    "Filesystem MCP servers are NOT recommended because Claude Code's MCP",
    "security restricts them to the project root directory.",
    "",
    "For remote repos without local clones (Phase B), see docs/reviews/."
  ]
}
